import { ComparsaDirective } from './comparsa.directive';

describe('ComparsaDirective', () => {
  it('should create an instance', () => {
    const directive = new ComparsaDirective();
    expect(directive).toBeTruthy();
  });
});
